{
  "name": "chars-hijack",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "build:standalone": "bun run build && bun run build-standalone.ts",
    "build:windows": "bun run build && bun run build-standalone.ts --platform=windows",
    "build:linux": "bun run build && bun run build-standalone.ts --platform=linux",
    "build:all": "bun run build:standalone && bun run build:windows && bun run build:linux"
  },
  "dependencies": {
    "@vue-flow/background": "^1.3.2",
    "@vue-flow/controls": "^1.1.3",
    "@vue-flow/core": "^1.48.2",
    "smol-toml": "^1.6.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vite-plugin-static-copy": "^3.2.0",
    "vue-tsc": "^2.0.0"
  }
}
